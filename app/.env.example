# Copy this file to .env.local for local development, and add the same two
# variables in the Cloudflare Pages dashboard (Settings -> Environment variables).
# Leave them blank and the app still works — it just saves to your browser only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
